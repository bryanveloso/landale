{
  "name": "landale",
  "version": "0.1",
  "module": "index.ts",
  "scripts": {
    "dev": "bun --filter '*' dev",
    "format": "prettier --write .",
    "cache-emotes": "bun run scripts/cache-emotes.ts"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "^3.5.3",
    "prettier-plugin-tailwindcss": "^0.6.11"
  }
}
