{
  "extends": "../../tsconfig.package.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "lib": ["ESNext", "DOM"], // Support both environments
    "types": ["node"] // But primarily Node-focused
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "**/*.test.ts"]
}
