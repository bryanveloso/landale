{
  "extends": "../../tsconfig.package.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "lib": ["ESNext"], // Node only
    "types": ["node"] // Uses Node fs APIs
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules"]
}
